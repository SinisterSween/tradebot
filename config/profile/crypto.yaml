# config/profile/crypto.yaml
timezone: UTC

execution:
  be_lock_min: 15
  be_req_profit_ticks: 1
  max_hold_min: 45
  hard_cutoff_R: -0.25
  hard_cutoff_min: 10
  min_minutes_to_window_end: 10

# Leave strategy mostly empty in the crypto profile.
# Universes should define strategy.name + knobs.
strategy:
  session_windows:
    - ["00:00", "23:59"]

ibkr:
  what_to_show: TRADES
  bar_size_secs: 60
