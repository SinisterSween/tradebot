# config/overrides/clean_v1.yaml
execution:
  be_lock_min: 10              # was 12
  be_req_profit_ticks: 1       # was 2
  max_hold_min: 30
  min_minutes_to_window_end: 35
  hard_cutoff_R: -0.25         # NOTE: currently not wired in code; harmless to keep
  hard_cutoff_min: 10          # NOTE: currently not wired; harmless to keep
  cooldown_bars: 15