execution:
  be_lock_min: 10          # wait 10 min before BE
  be_req_profit_ticks: 2   # needs to move 2 ticks ($0.02) in our favor
  max_hold_min: 30
  hard_cutoff_R: -0.25
  hard_cutoff_min: 10
  min_minutes_to_window_end: 20
